when:
  - event: ["pull_request"]
    branch: ["main"]

engine: "nixery"

dependencies:
  nixpkgs:
    - nodejs

steps:
  - name: "Install dependencies"
    command: "npm ci"
  
  - name: "Run CI checks"
    command: "npm run ci"
